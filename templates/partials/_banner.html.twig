 <header class="header_nav" >
     <nav class="navbar navbar-expand-lg">
        <div class="container-fluid d-flex"  id="navbare">
           <div class="me-auto"><a href="{{path('app_home')}}"><img class="navbar-brand" src="{{ asset('img/EcoRide.png') }}"  alt="Logo"></a></div> 
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class=" collapse navbar-collapse " id="navbarSupportedContent">

                <ul class="navbar-nav ">
                    

                        {% if not app.user %}
                          <li class="nav-item px-5"><a class="nav-link" href="{{path('app_register')}}"><i class="bi bi-person-add"></i>  M'INSCRIRE</a></li>
                          <li class="nav-item px-5"><a class="nav-link" href="{{ path('app_login') }}"><i class="bi bi-person"></i> ME CONNECTER</a></li>
                        {% else %}   
                    
                        <li class="nav-item px-5"><a class="nav-link" href="{{ path('app_profile') }}">Profil</a></li>
                      
                        <li class="nav-item px-5"><a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a></li>
                        {% endif %}
         

                      <li class="nav-item px-5">
                        <a class="nav-link " aria-current="page" href="{{ path('app_trip_index') }}">
                        <i class="bi bi-car-front-fill"></i>  
                        TRAJETS</a>
                      </li>
                      
                      <li class="nav-item px-5">
                        <a class="nav-link {% if app.current_route == 'app_contact' %} active{% endif %}" href="{{ path('app_contact') }}">
                          <i class="bi bi-envelope-open"></i>
                          CONTACT
                        </a>
                      </li>
                    
                 

                </ul>
            </div>
        </div>
      </nav>
    </header>